---
sidebar_position: 9
title: Deploy the Docs (Netlify)
---

- **Build command**: `npm run build`
- **Publish directory**: `build`
- If deploying under a subpath, set `baseUrl` in `docusaurus.config.js` accordingly (e.g., `/docs/`).
